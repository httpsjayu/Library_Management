spring:
  application:
    name: User-Service

  boot:
    admin:
      client:
        url: http://localhost:1111/


  datasource:
    url: jdbc:mysql://localhost:3306/Library_Mng
    username: root
    password: Jay@271203
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: 2712

jwt:
  secret: LibraryManagementSystem@2026#JWT$SecretKey!SecureRandom987654321ABCDEF

user-service:
  messages:
    UserReg: User Registered Successfully
    UserRegErr: Failed to Register User
    UserLogin: User Login Successfully
    UserLoginErr: Invalid Email or Password
